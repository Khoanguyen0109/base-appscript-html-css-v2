{
  "name": "base-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build client/index.html --no-minify --no-source-maps --no-cache",
    "move": "node util.js",
    "build-appscript": "npm run build && npm run move",
    "push": "clasp push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel-bundler": "^1.12.5",
    "parcel-plugin-inliner": "^1.0.16",
    "posthtml": "^0.16.6"
  }
}
